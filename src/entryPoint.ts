// // remove everything from body and head
// document.body.innerHTML = '';
// document.head.innerHTML = '';
// document.body.appendChild(document.createElement('div')).id = 'app';
// // Now we can render the main app
import("./main")
